use std::env;
use std::fs::File;

fn main() {
    // read a markdown file and output an HTML file
    // put getting file as a func
    // put reading the file and converting as another fun
    // call both these funs in main
    let file = File::open(filepath);
    println!("Hello, world!");
}
